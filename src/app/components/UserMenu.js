import React from "react";

export default class UserMenu extends React.Component {
    render() {
        return(
                <div class="UserMenu">
                USER-MENU
            </div>
        );
    }
}
