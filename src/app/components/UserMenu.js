import React from "react";

export default class UserMenu extends React.Component {
    render() {
        return(
                <div className="user-menu">
                USER-MENU
            </div>
        );
    }
}
