import React from "react";

export default class UserMenu extends React.Component {
    render() {
        return(
                <div className="UserMenu">
                USER-MENU
            </div>
        );
    }
}
